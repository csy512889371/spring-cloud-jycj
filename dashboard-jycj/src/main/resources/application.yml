spring:
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
      health:
        show-details: ALWAYS

#端点 默认值为health,info
#management:
#  endpoints:
#    web:
#      exposure:
#        include: hystrix.stream,health,info
